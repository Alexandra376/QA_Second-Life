package pages;

import org.openqa.selenium.WebDriver;
import org.openqa.selenium.support.ui.WebDriverWait;

public class FurnitureAndHomeDecorPage extends BasePage {
    public FurnitureAndHomeDecorPage(WebDriver driver, WebDriverWait wait) {
        super(driver, wait);
    }
}
