package pages;

import org.openqa.selenium.WebDriver;
import org.openqa.selenium.support.ui.WebDriverWait;

public class CreateNewOfferPage extends BasePage {
    public CreateNewOfferPage(WebDriver driver, WebDriverWait wait) {
        super(driver, wait);
    }
}
