package pages;

import org.openqa.selenium.WebDriver;
import org.openqa.selenium.support.ui.WebDriverWait;

public class ElectronicsAndGadgetsPage extends BasePage {
    public ElectronicsAndGadgetsPage(WebDriver driver, WebDriverWait wait) {
        super(driver, wait);
    }
}
