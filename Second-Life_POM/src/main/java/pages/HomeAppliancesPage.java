package pages;

import org.openqa.selenium.WebDriver;
import org.openqa.selenium.support.ui.WebDriverWait;

public class HomeAppliancesPage extends BasePage {
    public HomeAppliancesPage(WebDriver driver, WebDriverWait wait) {
        super(driver, wait);
    }
}
