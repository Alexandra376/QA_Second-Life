package pages;

import org.openqa.selenium.WebDriver;
import org.openqa.selenium.support.ui.WebDriverWait;

public class PetsAndSuppliesPage extends BasePage {
    public PetsAndSuppliesPage(WebDriver driver, WebDriverWait wait) {
        super(driver, wait);
    }
}
