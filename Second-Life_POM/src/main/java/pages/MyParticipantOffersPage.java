package pages;

import org.openqa.selenium.WebDriver;
import org.openqa.selenium.support.ui.WebDriverWait;

public class MyParticipantOffersPage extends BasePage {
    public MyParticipantOffersPage(WebDriver driver, WebDriverWait wait) {
        super(driver, wait);
    }
}
