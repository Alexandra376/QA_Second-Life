package pages;

import org.openqa.selenium.WebDriver;
import org.openqa.selenium.support.ui.WebDriverWait;

public class OfferPlusAuctionPage extends BasePage {
    public OfferPlusAuctionPage(WebDriver driver, WebDriverWait wait) {
        super(driver, wait);
    }
}
